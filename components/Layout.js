import Head from 'next/head'
const Layout = ({}) => {
    return (    
        <main>
            <Head>
                <title>My NEct-JS Page</title>
            </Head>

        </main>
     );
}
 
export default Layout;